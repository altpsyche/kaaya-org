---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-8">
  <h2 class="text-2xl font-bold text-[--color-text] sm:text-3xl">{title}</h2>
  {subtitle && <p class="mt-2 text-[--color-text-muted]">{subtitle}</p>}
</div>
