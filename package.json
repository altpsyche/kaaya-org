{
  "name": "kaaya_org",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "npm run images && astro dev",
    "build": "astro build",
    "prebuild": "node scripts/images.mjs",
    "images": "node scripts/images.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build",
    "test": "playwright test --pass-with-no-tests",
    "gate": "npm run build && npm run gate:vocab && npm run gate:links && npm run gate:proxy",
    "gate:vocab": "node scripts/gate/banned-vocab.mjs",
    "gate:links": "node scripts/gate/dead-links.mjs",
    "gate:content": "node scripts/gate/content-audit.mjs",
    "gate:proxy": "node scripts/gate/proxy.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/typography": "^0.5.20",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@tailwindcss/vite": "^4.3.1",
    "wrangler": "^4.120.0",
    "yaml": "^2.9.0"
  }
}
