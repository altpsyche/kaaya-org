{
  "name": "kaaya_org",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build",
    "test": "playwright test --pass-with-no-tests",
    "gate": "astro build && npm run gate:vocab && npm run gate:links",
    "gate:vocab": "node scripts/gate/banned-vocab.mjs",
    "gate:links": "node scripts/gate/dead-links.mjs",
    "gate:content": "node scripts/gate/content-audit.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/typography": "^0.5.20",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@tailwindcss/vite": "^4.3.1",
    "yaml": "^2.9.0"
  }
}
